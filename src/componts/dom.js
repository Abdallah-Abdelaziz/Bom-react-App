import styled from "styled-components"

const logo = styled.div`
    height: 60px:
    background: red'
`
const load = styled.a`
    color: 
`

const dom = () => {
  return (
    <logo>dom</logo>
  )
}

export default dom